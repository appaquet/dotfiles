{ scope }:
{
  heading = "Testing Principles";
  content = ''
    * Verify outcomes (state, return values), not just that code runs
    * Test integration seams — bugs cluster at component boundaries
    * Include at least one invalid-input test per operation
  '';
}

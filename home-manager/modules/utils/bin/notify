#!/usr/bin/env bash

MESSAGE="${1:-"<no message>"}"

HOSTNAME=$(uname -n | tr '[:upper:]' '[:lower:] | sed 's/\.local//'')
if [[ "${HOSTNAME}" == "mbpapp"* ]]; then
  terminal-notifier -message "${MESSAGE}"
else
  ssh mbpapp.n3x.net notify "${MESSAGE}"
fi
